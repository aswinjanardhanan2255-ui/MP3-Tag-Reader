# MP3-Tag-Reader
MP3 Tag Reader is a C-based project that reads and extracts metadata (ID3 tags) from MP3 files, such as song title, artist name, album, year, genre and comments. It works by accessing the MP3 file data without changing the actual audio.
